@AGENTS.md

# Claude Code

Siga o `AGENTS.md` acima e as skills canônicas em `.agents/skills/` (`radial-fast-development`, `radial-safety`, `radial-handoff`). Nenhuma orquestração especial além disso: 1 agente principal por padrão, sem subagentes próprios.
